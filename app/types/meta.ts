/**
 * @fileoverview Re-exports shared types for backward compatibility.
 * All type definitions now live in shared/types.ts.
 *
 * @module types/meta
 */

export type {
  MetaTags,
  DiagnosticResult,
  Diagnostics,
} from "#shared/types";
